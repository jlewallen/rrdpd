<div class="graphs">
  <%
  var defaultGraph = model.defaultGraph()
  %>
  <h2><%= defaultGraph.title %></h2>
  <ul class="menu">
    <% jQuery.each(model.menu, function(i, option) { %>
      <li>
      <%= i %>:
      <ul class="submenu">
        <% jQuery.each(model.menu[i], function(i, option) { %>
          <li><a class="displays" href="#" data-key="<%= option.uri %>"><%= i %></a></li>
        <% }); %>
      </ul>
      </li>
    <% }); %>
  </ul>
  <div class="graph">
    <img src="<%= defaultGraph.image %>" />
  </div>
</div>
